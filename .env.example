# Copy this file to `.env.local`.
# The one variable here is optional: the site builds fine without it.

# Printed on the generated CV at /cv.pdf, and nowhere else.
# It lives here rather than in cv.config.ts so a phone number never ends up in
# a public git history. Leave it unset and the line is simply left off the CV.
CV_PHONE=
